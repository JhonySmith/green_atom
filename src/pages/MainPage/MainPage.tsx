import { ChartWidget } from "../../widgets/StatTable/ui/ChartWidget";

interface IProps {}

export const MainPage = (props: IProps) => {
  const {} = props;

  return (
    <>
      <ChartWidget />
    </>
  );
};
